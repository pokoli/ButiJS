/*
    File that hols the database configuration access.
*/

/*
   Change the exports to fit your neeeds. 
   
   - host: Specifies which server we want to use
   - database: Specifies which database we whant to use. 

*/
module.exports = {
    host: 'localhost',
    database : 'test',
}
